import SwiftUI

extension Color {
    /// #7c3aed — the violet used for a stop everywhere it appears: the numbered
    /// map pins, the trip list badges, and the reorder sheet. Kept in one place
    /// because three copies of the same literal had already started to drift.
    static let stopViolet = Color(red: 0.486, green: 0.227, blue: 0.929)
}
