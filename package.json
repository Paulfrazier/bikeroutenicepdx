{
  "name": "bikeroutenicepdx",
  "version": "0.1.0",
  "private": true,
  "description": "Portland Greenway Router — bike routing PWA using PBOT's authoritative greenway GIS",
  "license": "MIT",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "fetch:pbot": "tsx scripts/fetch-pbot.ts",
    "fetch:osm": "tsx scripts/fetch-osm.ts",
    "build:graph": "tsx scripts/build-graph.ts",
    "export:greenways": "tsx scripts/export-greenways.ts",
    "export:bike-network": "tsx scripts/export-bike-network.ts",
    "export:arterials": "tsx scripts/export-arterials.ts",
    "build:tiles": "tsx scripts/build-tiles.ts",
    "reconcile": "tsx scripts/reconcile.ts",
    "test:routes": "tsx tests/routes/run.ts",
    "compare:engines": "tsx scripts/compare-engines.ts",
    "typecheck:server": "npm --workspace server run typecheck",
    "typecheck:web": "npm --workspace web run typecheck",
    "check:parity": "tsx scripts/check-parity.ts",
    "check:data": "tsx scripts/check-data-sync.ts",
    "check": "npm run typecheck:server && npm run typecheck:web && npm run check:parity && npm run check:data",
    "dev:server": "npm --workspace server run dev",
    "dev:web": "npm --workspace web run dev"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "@turf/turf": "^7.1.0",
    "osmtogeojson": "^3.0.0-beta.5"
  }
}
