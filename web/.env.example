VITE_API_URL=/api
# Basemap: leave unset for the free OpenFreeMap hosted style (no API key).
# Set to a "*.pmtiles" path to self-host (build via `npm run build:tiles`).
# VITE_BASEMAP_URL=/portland.pmtiles
