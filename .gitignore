node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.local

# Fetched data — large, regenerable
data/pbot/*.geojson
data/pbot/*.zip
data/osm/
data/reconciled/*.pbf
data/reconciled/*.osm
data/reconciled/*.geojson
# Dated reconcile runs (large, regenerable OSM extracts)
data/reconciled/*/

# Basemap tile build — large, regenerable
data/sources/
data/tile_weights.tsv.gz
tools/

# Valhalla tiles
routing/valhalla_tiles/
routing/valhalla_tiles.tar
routing/*.gph

# PMTiles
web/public/*.pmtiles

# Test harness output
tests/results/
.gstack/
